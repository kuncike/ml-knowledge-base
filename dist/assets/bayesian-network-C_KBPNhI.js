const n=`# 贝叶斯网络 (Bayesian Network)

## 核心思想

贝叶斯网络（又称信念网络）是一个**有向无环图 (DAG)**，节点代表随机变量，边代表条件依赖关系。相比朴素贝叶斯的"全独立"假设，贝叶斯网络可以编码更复杂的依赖结构。

## 数学定义

一个贝叶斯网络包含：

1. **结构**：有向无环图 $G = (V, E)$
2. **参数**：每个节点的条件概率表 (CPT) $P(X_i \\mid \\text{Parents}(X_i))$

联合概率分布分解为：

$$P(X_1, X_2, \\ldots, X_n) = \\prod_{i=1}^{n} P(X_i \\mid \\text{Parents}(X_i))$$

## 条件独立性

贝叶斯网络的核心价值在于编码**条件独立性**，大幅减少参数数量：

- 朴素贝叶斯（全独立）：$O(n)$ 参数
- 全联合分布：$O(2^n)$ 参数
- 贝叶斯网络：$O(n \\cdot 2^k)$ 参数，其中 $k$ 是最大父节点数

## 三种基本结构

\`\`\`
1. 串行:  A → B → C     (A 和 C 在给定 B 时条件独立)
2. 分叉:  A ← B → C     (A 和 C 在给定 B 时条件独立)
3. 汇合:  A → B ← C     (A 和 C 边缘独立，但给定 B 时不独立)
\`\`\`

## 推理类型

- **因果推理**：$P(\\text{结果} \\mid \\text{原因})$
- **诊断推理**：$P(\\text{原因} \\mid \\text{结果})$
- **混合推理**：结合以上两种

## 参数学习

当结构和数据都已知时，可用**最大似然估计 (MLE)** 学习 CPT：

$$P(X_i = x \\mid \\text{Pa}(X_i) = \\mathbf{p}) = \\frac{\\text{count}(X_i = x, \\text{Pa}(X_i) = \\mathbf{p})}{\\text{count}(\\text{Pa}(X_i) = \\mathbf{p})}$$

## 结构学习

从数据中学习网络结构是 NP-hard 问题，常用方法：
- **基于约束**：通过条件独立性检验构建图
- **基于评分**：定义评分函数（如 BIC），搜索最优结构
- **混合方法**：结合以上两种

## 应用场景

- 医疗诊断（症状 ↔ 疾病）
- 故障诊断与根本原因分析
- 推荐系统（用户偏好建模）
`;export{n as default};
